interface:
  display_name: "Waveform Brain"
  short_description: "Repo-specific patterns and workflows for waveform_brain"
  default_prompt: "Use the waveform_brain repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true